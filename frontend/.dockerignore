# Ignore node_modules (installed inside Docker)
node_modules/
npm-debug.log

# Ignore local build artifacts (they will be built in Docker)
build/
dist/

# Ignore IDE or editor-specific files
.vscode/
.idea/

# Ignore Dockerfile and docker-compose file (not needed in container)
Dockerfile
docker-compose.yml

# Ignore environment files (if you are managing them differently)
.env
